<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">FabricCutter.UI</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">      
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="brackets" Match="NavLinkMatch.All">
                <span class="oi oi-transfer" aria-hidden="true"></span> Cleaning brackets
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="pairs-en">
                <span class="oi oi-excerpt" aria-hidden="true"></span> Cleaning alphabeth
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
